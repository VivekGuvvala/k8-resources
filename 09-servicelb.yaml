apiVersion: v1
kind: Service
metadata:
  name: demo-lb
spec:
  type: LoadBalancer
  selector:
    project: roboshop
    component: frontend
    environment: dev
    purpose: LB
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 31281